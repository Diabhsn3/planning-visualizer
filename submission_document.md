# מסמך הגשה: פרויקט גמר - Visualizing Plans of Domain-Independent Planners using LLMs

מסמך זה מפרט את תכנון הבדיקות, פיילוט המשתמשים, סטטוס הפיתוח, האתגרים ולוח הזמנים להמשך הפרויקט, בהתאם לדרישות ההגשה.

## 1. תכנון פיילוט עם משתמשים אמיתיים (User Pilot Testing)

מטרת הפיילוט היא להעריך את השימושיות, היעילות והערך המוסף של המערכת עבור משתמשי קצה, בדגש על הפיכת פלטי תכנון (PDDL) מורכבים לברורים ונגישים יותר.

### אוכלוסיית היעד
הפיילוט יבוצע מול שתי קבוצות משתמשים עיקריות:
1. **סטודנטים/חוקרים בתחום הבינה המלאכותית והתכנון (AI Planning):** בעלי ידע בסיסי ב-PDDL, שיוכלו להעריך את הדיוק והתועלת של הוויזואליזציה בהשוואה לקריאת טקסט גולמי.
2. **משתמשים ללא רקע מוקדם בתכנון אוטומטי:** כדי לבחון האם המערכת מצליחה להנגיש את התחום ולהפוך את רצף הפעולות למובן גם למי שאינו מומחה.

### מטלות הפיילוט (Tasks)
כל משתתף יתבקש לבצע סדרת מטלות במערכת:
1. **העלאת דומיין ובעיה מובנים (Built-in):** בחירת דומיין קיים (למשל Blocks World או Logistics), הרצת התכנון וצפייה בוויזואליזציה.
2. **העלאת דומיין מותאם אישית (Custom Domain):** שימוש בממשק החדש להעלאת קבצי PDDL מותאמים אישית (באמצעות קובץ או טקסט), ובחינת יכולת המערכת לייצר ויזואליזציה אוטומטית מבוססת LLM.
3. **ניתוח תוכנית (Plan Analysis):** מעבר צעד-אחר-צעד על הפתרון שנוצר, זיהוי פעולות ספציפיות, והבנת שינויי המצב (State Transitions) בכל שלב.
4. **השוואת מצבי רינדור (Render Modes):** מעבר בין תצוגה בסיסית (Basic) לתצוגה מבוססת LLM (עבור דומיינים מובנים) והערכת ההבדלים בבהירות.

### מדדי הערכה (Evaluation Metrics)
הערכת הפיילוט תתבצע באמצעות שילוב של מדדים כמותיים ואיכותניים:
* **זמן השלמת מטלה (Time on Task):** מדידת הזמן שלוקח למשתמש להבין את הפתרון שנוצר.
* **שיעור הצלחה (Success Rate):** אחוז המשתמשים שהצליחו להשלים את המטלות ללא עזרה.
* **שאלון שימושיות (SUS - System Usability Scale):** שאלון סטנדרטי שיועבר בסיום הפיילוט למדידת שביעות רצון וקלות השימוש.
* **ראיונות קצרים/משוב פתוח:** איסוף תובנות על חוויית המשתמש, נקודות תורפה בממשק, והצעות לשיפור (למשל, האם הטרנספורמציה של ה-LLM הייתה מדויקת ומובנת).

---

## 2. תכנון מפורט של בדיקות המערכת (System Testing)

בדיקות המערכת נועדו להבטיח את יציבותה, אמינותה וביצועיה, במיוחד לאור השילוב של מנועי תכנון חיצוניים (Fast Downward) ומודלי שפה (LLMs).

### בדיקות יחידה (Unit Testing)
* **רכיבי Frontend:** בדיקת רכיבי ה-React (כגון `StateCanvas`, `Visualizer`) כדי לוודא שהם מרונדרים כראוי ומגיבים נכון לשינויי מצב (State).
* **פונקציות עזר (Utilities):** בדיקת פונקציות ניתוח ה-PDDL, חילוץ המצבים, וניהול השגיאות.

### בדיקות אינטגרציה (Integration Testing)
* **ממשק Frontend-Backend:** וידוא שקריאות ה-API (למשל `generateStates`, `llmGenerateTransformer`) מעבירות נתונים כהלכה ומטפלות בשגיאות רשת.
* **אינטגרציה עם Fast Downward:** בדיקה שהמערכת מצליחה להריץ את מנוע התכנון, לקרוא את קבצי הפלט (SAS/Plan), ולתרגם אותם למבנה נתונים תקין.
* **אינטגרציה עם LLMs (Claude/Gemini):** וידוא שהבקשות למודלי השפה נשלחות בפורמט הנכון, ושהקוד המיוצר (Transformer/Renderer) עובר קומפילציה ופועל כנדרש ללא שגיאות אבטחה או תחביר.

### בדיקות קצה לקצה (End-to-End Testing)
* **תרחיש זרימה מלא (Happy Path):** בחירת דומיין, הגדרת בעיה, הרצת התכנון, קבלת הפתרון, ורינדור ויזואלי מלא של כל השלבים.
* **תרחישי קצה ושגיאות (Edge Cases & Error Handling):** 
  * העלאת קבצי PDDL לא תקינים או חסרים.
  * בעיות שאינן ניתנות לפתרון (Unsolvable Problems).
  * חריגות זמן ריצה (Timeouts) של מנוע התכנון או ה-LLM.
  * טיפול במקרים בהם ה-LLM מייצר קוד שגוי.

---

## 3. סטטוס הפיתוח וההטמעה (Development Status)

המערכת נמצאת בשלב פיתוח מתקדם (גרסה V2), וכוללת ארכיטקטורה מלאה של Frontend (React/Vite) ו-Backend (Node.js/Express + Python).

### יכולות שהושלמו:
* **תשתית תכנון:** שילוב מלא של מנוע Fast Downward להרצת בעיות PDDL.
* **דומיינים מובנים:** תמיכה במספר דומיינים קלאסיים (Blocks World, Logistics, Depot, Gripper, Hanoi, Rovers, Satellite) עם ויזואליזציה ייעודית.
* **ממשק משתמש (UI/UX):** עיצוב מודרני ואינטראקטיבי, כולל אנימציות, תצוגת שלבים (Step-by-Step), ומעבר חלק בין מצבי תצוגה.
* **תמיכה בדומיינים מותאמים אישית (Custom Domains):** פיתוח מנגנון המאפשר למשתמשים להעלות קבצי PDDL משלהם (או להזין טקסט ישירות).
* **שילוב LLM (Phase 1-4):** יישום צינור נתונים (Pipeline) אוטומטי המשתמש במודלי שפה (Claude/Gemini) כדי לנתח דומיינים חדשים, לייצר עבורם קוד טרנספורמציה (State Transformer) וקוד רינדור (Canvas Renderer) בזמן אמת.

### סטטוס נוכחי:
המערכת פועלת וניתנת להרצה מקומית. בוצעו לאחרונה שיפורים משמעותיים בממשק המשתמש, כולל איחוד תפריטי ההגדרות (Configure Menu) והפיכת תהליך הרינדור מבוסס ה-LLM לאוטומטי ושקוף עבור דומיינים מותאמים אישית.

---

## 4. אתגרים מרכזיים (Challenges)

1. **אמינות קוד מיוצר LLM:** האתגר הגדול ביותר הוא להבטיח שהקוד (TypeScript/JavaScript) שמיוצר על ידי מודלי השפה עבור דומיינים חדשים יהיה תקין, בטוח להרצה, וייצג נאמנה את הלוגיקה של ה-PDDL.
2. **ביצועים וזמני תגובה:** הרצת Fast Downward ולאחריה קריאות ל-API של LLM עשויות לקחת זמן. יש צורך בניהול ציפיות המשתמש (UI Feedback) ואופטימיזציה של זמני ההמתנה.
3. **תאימות סביבות (Environment Compatibility):** התמודדות עם קשיי קימפול של Fast Downward בסביבות מסוימות (למשל macOS עם Xcode 15+), הדורשת מנגנוני Fallback (תוכניות מחושבות מראש).
4. **מורכבות ה-PDDL:** התמודדות עם תחביר PDDL מגוון ושגיאות משתמש בהזנת קבצים מותאמים אישית, תוך מתן הודעות שגיאה ברורות ומדויקות במקום קריסת המערכת.

---

## 5. לו"ז מפורט להמשך (Future Timeline)

| שבוע | משימות עיקריות | תוצרים צפויים |
|---|---|---|
| **שבוע 1** | השלמת בדיקות יחידה ואינטגרציה (Unit & Integration Tests). | כיסוי קוד בסיסי, תיקון באגים קריטיים בממשק ה-LLM. |
| **שבוע 2** | הכנות לפיילוט: כתיבת תסריטי בדיקה, גיוס משתתפים, והקמת סביבת בדיקות יציבה (Staging). | מסמך תסריטי פיילוט מאושר, סביבת מערכת מוכנה למשתמשים. |
| **שבוע 3** | ביצוע פיילוט משתמשים (User Pilot Testing) ואיסוף נתונים. | נתונים גולמיים, הקלטות, ושאלונים מלאים מהמשתתפים. |
| **שבוע 4** | ניתוח תוצאות הפיילוט, הפקת לקחים, ויישום תיקונים ושיפורים בממשק ובמנגנון ה-LLM בהתאם למשוב. | גרסת מערכת מעודכנת (Release Candidate) הכוללת שיפורי שימושיות. |
| **שבוע 5** | בדיקות קצה לקצה (E2E) סופיות, בדיקות עומסים, והכנת תיעוד טכני ומדריך למשתמש. | מערכת יציבה ומוכנה להגשה, תיעוד מלא. |
| **שבוע 6** | פריסה לשרת ייצור (Production Deployment), הכנת מצגת סיום והגשת הפרויקט. | מערכת באוויר, מצגת מוכנה, הגשה סופית. |
